# WireGuard Admin Panel

یک اپلیکیشن دسکتاپ مدیریت WireGuard با Electron برای ویندوز و لینوکس.

## ویژگی‌ها

- 🔐 احراز هویت با JWT Token
- 👥 مدیریت کلاینت‌های WireGuard (ایجاد، حذف، مشاهده)
- 📊 نمایش آمار ترافیک و تاریخ انقضا
- 📱 تولید QR Code برای اتصال سریع
- 💾 دانلود فایل کانفیگ (.conf)
- 🌐 پشتیبانی از زبان فارسی (RTL)
- 🎨 رابط کاربری تیره و مدرن

## نصب و اجرا

### پیش‌نیازها

- Node.js 18 یا بالاتر
- npm یا yarn

### نصب Dependencies

```bash
npm install
```

### اجرا در حالت Development

```bash
npm run dev
```

این دستور اپلیکیشن را با DevTools باز می‌کند.

### Build برای Production

#### Windows (NSIS Installer)

```bash
npm run build:win
```

خروجی در پوشه `dist/` قرار می‌گیرد.

#### Linux (AppImage & deb)

```bash
npm run build:linux
```

### اجرای معمولی

```bash
npm start
```

## استفاده

### 1. نصب سرور

ابتدا باید اسکریپت تانل WireGuard را روی سرور خارج خود نصب کنید:

```bash
wget https://raw.githubusercontent.com/Mehrdad-Hooshmand/wireguard-tunnel/main/install-tunnel-unified.sh
chmod +x install-tunnel-unified.sh
sudo ./install-tunnel-unified.sh
```

گزینه 1 (Foreign Server) را انتخاب کنید و username و password دلخواه را وارد کنید.

### 2. ورود به پنل

1. اپلیکیشن را باز کنید
2. آدرس سرور را وارد کنید (مثال: `https://your-server.com`)
3. نام کاربری و رمز عبوری که در نصب وارد کردید را بزنید
4. روی "ورود" کلیک کنید

### 3. مدیریت کلاینت‌ها

#### ایجاد کلاینت جدید

1. روی دکمه "+ کلاینت جدید" کلیک کنید
2. نام کلاینت را وارد کنید (الزامی)
3. محدودیت ترافیک (گیگابایت) - اختیاری
4. مدت اعتبار (روز) - اختیاری
5. روی "ایجاد کلاینت" کلیک کنید

#### مشاهده QR Code

روی دکمه "QR Code" در کارت کلاینت کلیک کنید. QR Code برای اسکن با موبایل نمایش داده می‌شود.

#### دانلود کانفیگ

روی دکمه "دانلود" کلیک کنید. فایل `.conf` دانلود می‌شود که می‌توانید در اپلیکیشن WireGuard استفاده کنید.

#### حذف کلاینت

روی دکمه "حذف" کلیک کنید و تایید کنید.

## ساختار پروژه

```
wireguardadmin/
├── src/
│   ├── main/
│   │   └── main.js           # Electron main process
│   ├── renderer/
│   │   ├── index.html        # صفحه اصلی
│   │   ├── styles.css        # استایل‌ها
│   │   └── app.js            # لاجیک UI
│   └── services/
│       └── api.js            # API service
├── package.json
└── README.md
```

## API Backend

این اپلیکیشن به یک FastAPI backend نیاز دارد که endpoints زیر را ارائه دهد:

### Authentication

- `POST /api/login` - ورود با username و password

### Clients

- `GET /api/clients` - دریافت لیست کلاینت‌ها
- `POST /api/clients` - ایجاد کلاینت جدید
- `DELETE /api/clients/{id}` - حذف کلاینت
- `GET /api/clients/{id}/config` - دریافت کانفیگ
- `GET /api/clients/{id}/qr` - دریافت QR code

تمام endpoints جز `/api/login` نیاز به JWT token در header دارند:

```
Authorization: Bearer <token>
```

## تنظیمات Build

Build settings در `package.json` قرار دارد:

- **Windows**: NSIS installer با icon و نام نمایشی
- **Linux**: AppImage و deb package
- **Compression**: Maximum برای کاهش حجم

## توسعه

### اضافه کردن ویژگی جدید

1. API endpoint جدید را در `src/services/api.js` اضافه کنید
2. UI component را در `src/renderer/` بسازید
3. Event handler را در `src/renderer/app.js` اضافه کنید

### Debug

برای دیدن console log ها و debug کردن:

```bash
npm run dev
```

DevTools به صورت خودکار باز می‌شود.

## مشکلات رایج

### خطای اتصال به سرور

- مطمئن شوید آدرس سرور صحیح است
- بررسی کنید که سرور در حال اجراست
- فایروال و SSL certificate را چک کنید

### خطای authentication

- username و password را دوباره بررسی کنید
- مطمئن شوید backend در حال اجراست
- لاگ‌های سرور را برای مشکلات JWT چک کنید

### مشکل در build

- `node_modules` را پاک کنید و دوباره `npm install` کنید
- نسخه Node.js را بررسی کنید (18+)
- فضای کافی روی دیسک داشته باشید

## لایسنس

MIT

## ارتباط با ما

برای گزارش مشکلات و پیشنهادات از GitHub Issues استفاده کنید.
